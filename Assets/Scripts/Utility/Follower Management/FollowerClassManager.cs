using System;
using System.Threading;
using UnityEngine;

public class FollowerClassManager : MonoBehaviour
{
    /*
    Type[] baseClassTypes = { typeof(Bandit), typeof(Warrior), typeof(Druid) };
    Type randomClass = baseClassTypes[Random.Range(0, baseClassTypes.Length)];
    followerGameObject.AddComponent(randomClass);
    */
}
