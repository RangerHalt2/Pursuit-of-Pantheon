using UnityEngine;

public class GodSkillPointsSystem : MonoBehaviour
{
    //This should handle the skill points to upgrade the god between runs.
    //Leave this script to have getters/setters and add/subtract functions so we can have events add points or take the points when they upgrade something.
    //As we decide more definitively later.
}
