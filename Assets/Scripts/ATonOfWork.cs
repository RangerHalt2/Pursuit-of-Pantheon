using UnityEngine;

public class ATonOfWork : MonoBehaviour
{

    //I'm doing a ton!

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
